export interface menuDrinkItem {
    id: string;
    title: string;
    small: number;
    large: number;
}
export interface menuFoodItem {
    id: string;
    title: string;
    details: string;
    price: number;
}